# ── Supabase (project: Navinta-final, shared for now) ─────────────
NEXT_PUBLIC_SUPABASE_URL=https://iqfrjomoggddxwteuigk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_1mhtWKPAexk6-ycdAuR4sg_hdil-hlj

# Service-role key — SERVER ONLY, never expose to the browser.
# Supabase dashboard → Project Settings → API → service_role secret.
SUPABASE_SERVICE_ROLE_KEY=

# ── App token encryption ──────────────────────────────────────────
# 32-byte key (base64 or hex) used to AES-256-GCM encrypt stored Google
# tokens at rest. Generate one:  openssl rand -base64 32
APP_ENCRYPTION_KEY=

# ── Supabase (project: Navinta-final, shared for now) ─────────────
# Both values are public/safe for the browser. That's ALL the web app needs —
# the service-role key and encryption key now live in the Supabase Edge
# Function (jarvis-store-connection), not here.
NEXT_PUBLIC_SUPABASE_URL=https://iqfrjomoggddxwteuigk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_1mhtWKPAexk6-ycdAuR4sg_hdil-hlj

# Secrets that used to be here (SUPABASE_SERVICE_ROLE_KEY, APP_ENCRYPTION_KEY)
# are set on Supabase instead — see supabase/functions/jarvis-store-connection.
